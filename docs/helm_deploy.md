# Deploy Helm on Raspberry Pi cluster

Deploying helm on your cluster is easy. From your _/home/pi_ directory, execute the helm installation script.

```./ARMadillo/addons/helm/helm_install_rbac.sh```
